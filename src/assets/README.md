# Brand assets

Source files (`.psd`, `.ai`) live in `src/source/` — never imported into the site.
Web-ready exports in `illustrations/` and `logos/`.

## Palette (locked from `ColorPalette.ai`)

| Token | Hex | Use |
|---|---|---|
| `--paula-ink` | `#2a3818` | Primary text, dark surfaces |
| `--paula-cream` | `#faf1d8` | Default page background |
| `--paula-honey` | `#fdcd88` | Accent / warm CTA |
| `--paula-stone` | `#847e80` | Muted text / dividers |
| `--paula-blue` | `#28618c` | Link / cool accent |
| `--paula-plum` | `#774a64` | Section accent |
| `--paula-leaf` | `#279145` | Confirmation / positive |
| `--paula-olive` | `#8e6f2a` | Quiet accent |
| `--paula-iris` | `#5a4a75` | Section accent |

## Illustration inventory

- **VerticalHeads** — stacked overlapping profile silhouettes, watercolor+grain. Hero candidate.
- **GroupSupervisionHeads / indiviualHeads / supervision** — variants for Work/Supervision pages.
- **infininity** — three-tone watercolor infinity loop. Logo/motif candidate.
- **HandsGraphic2 / armTouch** — hands almost touching. About / connection sections.
- **QuoteMarks** — testimonial decoration.
- **Eyes / WaterColorLips / WaterColorLips 2** — face details.
- **Clouds / HorizontalLine** — section dividers / atmosphere.
- **TheraMediumPost / ThereaMedium** — blog/article headers if we add a writing section.

## Logos

- `Wordmark` (dark on cream) + `Wordmark_KO` (cream on dark) — primary
- `Horizontal` — wordmark + mark lockup, for header
- `Square` — favicon / social avatar (SVG available)
