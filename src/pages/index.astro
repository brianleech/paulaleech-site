---
import Layout from '../layouts/Layout.astro';
import headRose from '../assets/illustrations/HeadRose.png';
import headSage from '../assets/illustrations/HeadSage.png';
import infinity from '../assets/illustrations/infininity.png';
import hands from '../assets/illustrations/HandsGraphic2.png';
import quotesLeft from '../assets/illustrations/LeftQuote.png';
import quotesRight from '../assets/illustrations/RightQuote.png';
import bookMockup from '../assets/illustrations/BookMockup.png';
import clouds from '../assets/illustrations/Clouds.png';
const BASE = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<Layout title="Paula Leech — Sex Therapy, Supervision & Courses" headerCredentials={true}>

  <!-- HERO: huge cropped heads flanking the wordmark, type top-left -->
  <section class="relative" style="min-height: 78vh;">
    <!-- LEFT head (rose). Outer = JS momentum; inner = CSS ambient float. -->
    <div class="pl-momentum pl-fit-vertical absolute pointer-events-none" data-scroll="0.18" data-resize="0.4"
      style="right: calc(50% - 80px); z-index: 1; will-change: transform;">
      <img
        src={headRose.src}
        alt=""
        aria-hidden="true"
        class="pl-float-slow"
        style="
          display: block;
          width: clamp(360px, 42vw, 620px);
          height: auto;
          mix-blend-mode: multiply;
          opacity: 1;
        "
      />
    </div>

    <!-- RIGHT head (sage). Outer = JS momentum (opposite scroll bias); inner = CSS ambient float. -->
    <div class="pl-momentum pl-fit-vertical absolute pointer-events-none" data-scroll="-0.12" data-resize="-0.4"
      style="left: calc(50% - 80px); z-index: 1; will-change: transform;">
      <img
        src={headSage.src}
        alt=""
        aria-hidden="true"
        class="pl-float-slow-rev"
        style="
          display: block;
          width: clamp(360px, 42vw, 620px);
          height: auto;
          mix-blend-mode: multiply;
          opacity: 1;
        "
      />
    </div>

    <div class="relative z-10 mx-auto max-w-[1400px] px-12 lg:px-20 pt-10 md:pt-14" style="padding-top: calc(100px + 2.5rem);">
      <div class="pl-reveal">
        <h1
          style="
            font-family: var(--font-display);
            font-weight: 500;
            font-variation-settings: 'opsz' 144, 'SOFT' 60;
            font-size: clamp(3.5rem, 11vw, 10rem);
            line-height: 0.88;
            letter-spacing: -0.045em;
            color: var(--color-ink);
          "
        >
          P<span style="position: relative; display: inline-block;">au<span aria-label="pronouns she/her" style="
            position: absolute;
            right: 0;
            bottom: 100%;
            margin-bottom: -50px;
            text-align: right;
            font-family: var(--font-display);
            font-size: 0.12em;
            font-style: italic;
            font-weight: 400;
            letter-spacing: 0.01em;
            color: var(--color-ink);
            line-height: 1;
            white-space: nowrap;
            pointer-events: none;
          ">(she/her)</span></span>la<br/>Leech.
        </h1>
        <p style="
          font-family: var(--font-display);
          font-style: italic;
          font-weight: 400;
          font-size: clamp(1.5rem, 2.6vw, 2.1rem);
          line-height: 1.2;
          letter-spacing: -0.01em;
          color: var(--color-ink);
          margin-top: 1.5rem;
        ">
          Sex therapist, supervisor &amp; author.
        </p>
      </div>
    </div>
  </section>

  <!-- IN SHORT — conversational fragments -->
  <section id="in-short" class="mx-auto max-w-[1400px] px-12 lg:px-20 mt-12 md:mt-16 pl-reveal" style="margin-top: calc(4rem - 150px); position: relative; z-index: 5;">
    <h2 class="pl-label" style="margin-bottom: 3rem;">In short.</h2>
    <div class="pl-grid">
      <div class="col-span-12 md:col-span-3">
        <span class="pl-section-word">A bit about me.</span>
        <p class="mt-4 text-base" style="color: var(--color-ink-soft); line-height: 1.55;">
          Sex therapist, clinical supervisor, and educator. AASECT certified.
        </p>
      </div>
      <div class="col-span-12 md:col-span-3 mt-10 md:mt-0">
        <span class="pl-section-word">Where to find me.</span>
        <p class="mt-4 text-base" style="color: var(--color-ink-soft); line-height: 1.55;">
          Therapy: Massachusetts, Oregon, and New York.<br/>
          Supervision: worldwide.
        </p>
      </div>
      <div class="col-span-12 md:col-span-3 mt-10 md:mt-0">
        <span class="pl-section-word">Where I specialize.</span>
        <p class="mt-4 text-base" style="color: var(--color-ink-soft); line-height: 1.55;">
          Anxiety, shame, mismatched desire, and the work of finding pleasure&nbsp;again.
        </p>
      </div>
      <div class="col-span-12 md:col-span-3 mt-10 md:mt-0">
        <span class="pl-section-word">How it tends to go.</span>
        <p class="mt-4 text-base" style="color: var(--color-ink-soft); line-height: 1.55;">
          Virtual or in-person sessions. Steady, direct, and compassionate &mdash; with a little levity when it helps.
        </p>
      </div>
    </div>
  </section>

  <!-- LEAD STATEMENT — bracketed by giant teal/rose quotes -->
  <section class="mx-auto max-w-[1400px] px-12 lg:px-20 mt-40 md:mt-56 relative pl-reveal">
    <div class="relative">
      <!-- Opening quote (left, mid-height) -->
      <img src={quotesLeft.src} alt="" class="pl-quote-ornament absolute" style="
        top: 50%;
        left: -2rem;
        transform: translateY(-50%);
        width: clamp(140px, 17vw, 280px);
        height: auto;
      " />
      <!-- Closing quote (right, mid-height) -->
      <img src={quotesRight.src} alt="" class="pl-quote-ornament absolute" style="
        top: 50%;
        right: -2rem;
        transform: translateY(-50%);
        width: clamp(140px, 17vw, 280px);
        height: auto;
      " />

      <div class="pl-grid">
        <div class="col-span-12 md:col-start-3 md:col-span-8">
          <p style="
            font-family: var(--font-display);
            font-weight: 400;
            font-size: clamp(1.75rem, 3.4vw, 3rem);
            line-height: 1.2;
            letter-spacing: -0.018em;
            color: var(--color-ink);
          ">
            Anxiety, shame, disconnection &mdash;
            the parts of intimacy nobody taught us how to talk about.
            My work is to make the conversation possible,
            with steadiness and a little humor.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- LET'S WORK TOGETHER -->
  <section class="mx-auto max-w-[1400px] px-12 lg:px-20 mt-40 md:mt-56 pl-reveal">
    <div class="pl-grid items-start">
      <div class="col-span-12 md:col-span-4">
        <span class="pl-label">Services</span>
        <h2 style="font-family: var(--font-display); font-size: clamp(2.25rem, 4.5vw, 3.75rem); line-height: 0.95; margin-top: 0.75rem; letter-spacing: -0.025em;">
          Let's work<br/><em style="font-weight: 400;">together.</em>
        </h2>
        <a href={`${BASE}/work-with-paula`} class="pl-pill mt-8 inline-flex">See all →</a>
      </div>
      <div class="col-span-12 md:col-span-8 mt-10 md:mt-0">
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 4rem;">I.</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            Sex Therapy <span class="pl-label ml-3">MA · OR</span>
          </span>
          <span class="pl-label">Individuals &amp; couples →</span>
        </div>
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 4rem;">II.</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            Clinical Supervision <span class="pl-label ml-3">Worldwide</span>
          </span>
          <span class="pl-label">For therapists &amp; CST candidates →</span>
        </div>
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 4rem;">III.</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            Courses &amp; Trainings <span class="pl-label ml-3">Online</span>
          </span>
          <span class="pl-label">Scheduled &amp; on-demand →</span>
        </div>
      </div>
    </div>
  </section>

  <!-- COURSES SPOTLIGHT -->
  <section class="mx-auto max-w-[1400px] px-12 lg:px-20 mt-40 md:mt-56 pl-reveal">
    <div class="pl-grid items-start">
      <div class="col-span-12 md:col-span-4">
        <span class="pl-label">Now Booking</span>
        <h2 style="font-family: var(--font-display); font-size: clamp(2.25rem, 4.5vw, 3.75rem); line-height: 0.95; margin-top: 0.75rem; letter-spacing: -0.025em;">
          What's<br/><em style="font-weight: 400;">running now.</em>
        </h2>
        <a href={`${BASE}/courses`} class="pl-pill mt-8 inline-flex">Browse catalog →</a>
      </div>
      <div class="col-span-12 md:col-span-8 mt-10 md:mt-0">
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 5rem;">JUL 23</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            Treating Sexual Anxiety <span class="pl-label ml-3">Live · 6 hrs CE</span>
          </span>
          <span style="font-family: var(--font-mono); font-size: 0.95rem;">$400 →</span>
        </div>
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 5rem;">ON-DEMAND</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            Foundations for Sex Therapists <span class="pl-label ml-3">8 modules</span>
          </span>
          <span style="font-family: var(--font-mono); font-size: 0.95rem;">$295 →</span>
        </div>
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 5rem;">ON-DEMAND</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            The Anxiety + Intimacy Workshop <span class="pl-label ml-3">Self-paced</span>
          </span>
          <span style="font-family: var(--font-mono); font-size: 0.95rem;">$95 →</span>
        </div>
      </div>
    </div>
  </section>

  <!-- PRESS MINI-BAND -->
  <section class="mx-auto max-w-[1400px] px-12 lg:px-20 mt-40 md:mt-56 pl-reveal">
    <div class="pl-grid items-start">
      <div class="col-span-12 md:col-span-4">
        <span class="pl-label">Press &amp; Appearances</span>
        <h2 style="font-family: var(--font-display); font-size: clamp(2.25rem, 4.5vw, 3.75rem); line-height: 0.95; margin-top: 0.75rem; letter-spacing: -0.025em;">
          Where I've<br/><em style="font-weight: 400;">shown up.</em>
        </h2>
        <a href={`${BASE}/press`} class="pl-pill mt-8 inline-flex">Full archive →</a>
      </div>
      <div class="col-span-12 md:col-span-8 mt-10 md:mt-0">
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 5rem;">MAY 26</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            Where Should We Begin? <span class="pl-label ml-3">Esther Perel · Podcast</span>
          </span>
          <span class="pl-label">Listen →</span>
        </div>
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 5rem;">FEB 26</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            On Anxiety in the Bedroom <span class="pl-label ml-3">The Cut · Article</span>
          </span>
          <span class="pl-label">Read →</span>
        </div>
        <div class="pl-ledger">
          <span class="pl-label" style="min-width: 5rem;">OCT 25</span>
          <span style="font-family: var(--font-display); font-size: clamp(1.25rem, 2.2vw, 1.6rem);">
            Sex Therapy 101 <span class="pl-label ml-3">Sex With Emily · Podcast</span>
          </span>
          <span class="pl-label">Listen →</span>
        </div>
      </div>
    </div>
  </section>

  <!-- INLINE NEWSLETTER -->
  <section class="mx-auto max-w-[1400px] px-12 lg:px-20 mt-40 md:mt-56 pl-reveal">
    <div class="pl-grid items-center" style="padding: 3.5rem 2rem; background: color-mix(in srgb, var(--color-honey) 35%, transparent); border-radius: 1rem;">
      <div class="col-span-12 md:col-span-6">
        <p class="pl-label">The Newsletter</p>
        <h2 style="
          font-family: var(--font-display);
          font-style: italic;
          font-weight: 400;
          font-size: clamp(2rem, 4vw, 3.25rem);
          line-height: 1.05;
          letter-spacing: -0.02em;
          margin-top: 0.75rem;
        ">
          Occasional letters.<br/>No pitches.
        </h2>
        <p class="mt-4 text-base" style="color: var(--color-ink-soft); max-width: 42ch; line-height: 1.6;">
          Writing on intimacy, anxiety, and the work in between &mdash; sent when I have something worth saying.
        </p>
      </div>
      <div class="col-span-12 md:col-span-6 mt-8 md:mt-0">
        <form data-newsletter="inline" class="flex flex-col gap-3" onsubmit="event.preventDefault(); event.target.querySelector('[data-msg]').textContent = 'Thanks. Will let you know when this is wired up.';">
          <input
            type="email"
            required
            name="email"
            placeholder="you@example.com"
            aria-label="Email address"
            class="px-5 py-4 rounded-full"
            style="background: var(--color-cream); border: 1px solid color-mix(in srgb, var(--color-ink) 25%, transparent); color: var(--color-ink); font-size: 1.05rem;"
          />
          <button type="submit" class="pl-pill self-start" style="padding: 0.95rem 1.8rem; font-size: 0.95rem;">Subscribe</button>
          <span data-msg class="text-sm" style="color: var(--color-plum);"></span>
        </form>
      </div>
    </div>
  </section>

  <!-- BOOK CLOSING -->
  <section class="mx-auto max-w-[1400px] px-12 lg:px-20 mt-40 md:mt-56 mb-32 pl-reveal relative">
    <div class="pl-grid items-center">
      <div class="col-span-12 md:col-span-6">
        <div class="pl-parallax" data-speed="-0.05">
          <img src={bookMockup.src} alt="Overcoming Anxiety in Sex and Relationships — book mockup" class="w-full max-w-lg rounded-sm" style="box-shadow: 0 30px 60px -20px rgba(42,56,24,0.3);" />
        </div>
      </div>
      <div class="col-span-12 md:col-span-6 mt-10 md:mt-0">
        <span class="pl-label">The Book</span>
        <h2 style="
          font-family: var(--font-display);
          font-style: italic;
          font-weight: 400;
          font-size: clamp(2.25rem, 4.2vw, 3.75rem);
          line-height: 1.05;
          letter-spacing: -0.02em;
          margin-top: 0.75rem;
        ">
          Overcoming Anxiety<br/>in Sex and Relationships.
        </h2>
        <p class="mt-6 text-base" style="color: var(--color-ink-soft); max-width: 50ch; line-height: 1.65;">
          A comprehensive guide to intimate and emotional freedom. Tools, scripts, and the kind of permission most of us didn't grow up with.
        </p>
        <div class="mt-8 flex gap-3 flex-wrap">
          <a href={`${BASE}/book`} class="pl-pill">Read more →</a>
          <a href="https://www.routledge.com" class="pl-pill" style="background: var(--color-blue);" target="_blank" rel="noopener">Buy the book</a>
        </div>
      </div>
    </div>
  </section>

</Layout>
